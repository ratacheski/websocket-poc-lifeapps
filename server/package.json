{
  "name": "websocket-server-poc",
  "version": "1.0.0",
  "description": "Websocket Server Using Socket.io to proof sticky sessions concept",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon --env-file=config.env server.js"
  },
  "author": "Rafael Ratacheski",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.1",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
